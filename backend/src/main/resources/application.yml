# GENERATED BY CHAT GPT
---
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/archive
    username: archive
    password: archive
  sql:
    init:
      mode: never

server:
  port: 8080
